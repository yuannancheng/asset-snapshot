# TODO

## 现有功能总结

- 本地优先的 Tauri + React 资产快照应用，数据存储在 SQLite 单文件中。
- 支持平台和账户配置，账户类型固定为流动资产、非流动资产、负债。
- 支持新建、编辑、删除历史快照，汇总数据实时计算不入库。
- 支持停用账户；无历史数据的账户可以删除，无历史数据的平台可以连同子账户删除。
- 日期选择器支持直接输入、日历选择、年月切换，非法日期失焦后标红提示。
- 仪表盘展示总资产、可用资产、启用账户数、趋势图、平台分布图和最近历史快照。
- 趋势图支持最近 30 天、最近 3 个月、最近 1 年和自定义区间，并按范围自动使用日、周、月粒度。
- 已开始使用 Git；当前环境的 `.git` 是只读挂载点，临时使用 `.git-store` 作为分离 Git 目录。

## 后续待完成

- 分类管理完善：支持平台和账户改名、排序，保证历史数据仍按快照明细正确计算。
- 数据文件管理：实现数据文件入口，支持查看当前数据库位置、选择或创建数据文件、备份导出。
- 可选加密：支持无密码和 SQLCipher 加密模式，包含首次设置密码、解锁、修改密码。
- 安全策略：实现密码错误延迟策略，并明确不支持找回密码。
- 暗色模式：补齐设计书中建议的暗色主题和图表配色适配。
- 单例模式：重复运行应用不会打开新的实例，而是复用旧的窗口
- 注册文件后缀：指定 as 后缀文件的打开方式是本程序
- 打包验证：检查 release 包体积、启动时间、内存占用，保持轻量桌面工具定位。

## 工作记录

- 2026-05-16：实现趋势图时间筛选和日/周/月自动聚合；初始化分离式 Git 仓库 `.git-store`。
- Git 使用提示：在当前环境里执行命令时使用 `git --git-dir=.git-store --work-tree=. <command>`，直到只读 `.git` 挂载点被移除。

## 明确不做

- 不做流水记录。
- 不做消费分类。
- 不做预算系统。
- 不做自动导入。
