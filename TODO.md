# TODO

## 现有功能总结

- 本地优先的 Tauri + React 资产快照应用，数据存储在 SQLite 单文件中。
- 支持平台和账户配置，账户类型固定为流动资产、非流动资产、负债。
- 支持新建、编辑、删除历史快照，汇总数据实时计算不入库。
- 支持停用账户；无历史数据的账户可以删除，无历史数据的平台可以连同子账户删除。
- 支持数据文件管理：查看当前 SQLite 路径、选择/新建数据文件、导出备份，并记住最近使用的数据文件。
- 日期选择器支持直接输入、日历选择、年月切换，非法日期失焦后标红提示。
- 仪表盘展示总资产、可用资产、启用账户数、趋势图、平台分布图和最近历史快照。
- 趋势图支持最近 3 个月、最近 1 年、最近 3 年和自定义区间，直接展示所有快照节点不聚合。
- 暗色模式支持手动切换和系统主题自动跟随。
- 快照支持精确到分钟的时间戳。
- 平台可自定义配色（8 色预设 + 色码输入），图表优先使用自定义颜色。
- 平台与账户弹层增强分组展示、限制背景滚动、响应式宽度。
- 账户支持修改类型。
- 资产变动分析支持回车键快捷操作、自动追加空输入框、其余收入/支出自动计算并排在说明末尾。
- 历史快照列表新增备注和变动分析列，占满内容宽度。
- 可选 SQLCipher 加密。
- 已开始使用 Git。

## 后续工作

- 完善 README 中英文版本
- 继续修复 BUG 和细节优化

## BUG

## 功能优化

## 明确不做

- 不做流水记录。
- 不做消费分类。
- 不做预算系统。
- 不做自动导入。

## 工作记录

- 2026-05-16：实现趋势图时间筛选和日/周/月自动聚合；
- 2026-05-16：完善分类管理，支持平台和账户改名、上下排序；历史快照继续按账户明细关联并实时重算。
- 2026-05-16：调整平台与账户管理弹窗，把新建平台/账户放到管理列表末尾，并使用同规格的新建区域。
- 2026-05-16：日期选择器的年月选择改用标准下拉组件，统一弹层和选中态风格。
- 2026-05-24：完成数据文件管理入口；前端使用系统文件对话框选择/新建/备份数据文件，后端持久化当前数据文件路径。
- 2026-05-24：接入 Tauri 单例插件；重复运行会聚焦旧窗口，并支持通过 `.as` 文件关联启动或切换数据文件。
- 2026-05-24：修复无平台/账户时新建快照缺少引导、已启用账户卡片加号不可点击、图表时间范围仍含最近 30 天的问题。
- 2026-06-01：补齐暗色模式，新增明暗切换、主题持久化、语义色变量和图表暗色配色适配。
- 2026-06-01：完成快照变动分析第一版，支持收入/支出项目、多金额明细、自动补足差额、说明生成和 SQLite 持久化。
- 2026-06-04：完成可选 SQLCipher 加密。始终编译 SQLCipher，支持无密码和加密双模式。新增 DatabaseState 状态机（Locked/Unlocked），首次设置密码、解锁、修改密码、锁定功能。密码错误采用指数退避延迟（min(2^n, 30) 秒），明确不支持密码找回。前端新增 UnlockScreen 全屏解锁界面和数据文件管理的密码操作入口。
- 2026-06-07：全面优化推进。修复暗色模式系统主题跟随、趋势图直接展示所有节点、弹层背景滚动限制及响应式宽度；新增账户类型编辑、快照时间戳到分钟、平台自定义配色、平台账户分组增强展示；重构资产变动分析弹层（回车快捷、自动追加空输入、其余收入/支出自动计算排序）；历史快照列表满宽+备注+变动分析列；README 中英文版本。
